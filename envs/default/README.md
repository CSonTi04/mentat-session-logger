# Default Environment Template

This is a commit-safe template environment.

Do not store private campaign data here. Create a private environment with:
python -m mentat_session_logger init-env --name local
